'use client';

import ProjectsPageContent from '../../tools/projects/page';

export default function AdminProjectsPage() {
  // Render projects management directly within admin path
  return <ProjectsPageContent />;
}