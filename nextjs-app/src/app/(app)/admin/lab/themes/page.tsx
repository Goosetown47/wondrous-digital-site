'use client';

import ThemesPageContent from '../../../lab/themes/page';

export default function AdminLabThemesPage() {
  // Render the existing themes page content directly within admin path
  return <ThemesPageContent />;
}