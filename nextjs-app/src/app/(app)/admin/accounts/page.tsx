'use client';

import AccountsPageContent from '../../tools/accounts/page';

export default function AdminAccountsPage() {
  // Render accounts management directly within admin path
  return <AccountsPageContent />;
}