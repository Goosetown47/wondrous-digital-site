'use client';

import TypesPageContent from '../../tools/types/page';

export default function AdminTypesPage() {
  // Render types management directly within admin path
  return <TypesPageContent />;
}