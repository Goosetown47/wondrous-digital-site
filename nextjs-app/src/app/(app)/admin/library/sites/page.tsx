'use client';

import { LibrarySingleType } from '@/components/library/LibrarySingleType';

export default function AdminLibrarySitesPage() {
  return <LibrarySingleType type="site" title="Sites" />;
}