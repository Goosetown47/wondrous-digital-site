'use client';

import { LibrarySingleType } from '@/components/library/LibrarySingleType';

export default function AdminLibraryThemesPage() {
  return <LibrarySingleType type="theme" title="Themes" />;
}