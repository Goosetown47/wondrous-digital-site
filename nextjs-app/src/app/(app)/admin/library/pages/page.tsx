'use client';

import { LibrarySingleType } from '@/components/library/LibrarySingleType';

export default function AdminLibraryPagesPage() {
  return <LibrarySingleType type="page" title="Pages" />;
}